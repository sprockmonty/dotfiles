#!/bin/bash
# change location of working directory quickly using cwd
if [[ -z $(grep -E "^$1:" ~/bin/wd) ]] 
then
        echo "$1:$(pwd)" >> ~/bin/wd
else
        sed -i "/^$1/c $1:$(pwd)" ~/bin/wd
fi        
